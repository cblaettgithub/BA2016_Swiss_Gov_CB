 /*var dataset=function (csv, name) {
        return csv.map(function (d) {
            return d.supplier;
        })
    };*/

 //nur objekt zurück
    /*var dataset2=d3.csv(csv, function(d) {
        return {
            idSupplier: d.idSupplier,
            supplier: d.supplier,
        };
    }, function(error, rows) {
        console.log(rows);
    });*/

watchify app.js -o bundle.js -v
